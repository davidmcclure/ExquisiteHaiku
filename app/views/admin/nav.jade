div.container

  div.row

    span.span6

      div.nav
        a.nav(href='#') about
        a.nav(href='#') terms of service
        span.nav © 2012
          - var twitterUrl = 'https://twitter.com/clured'
          a.nav(href='#{twitterUrl}', target='_blank') @clured

      div.logo-block

        div.auth
          if user
            a.nav(href='/admin/logout') logout
            span.user= user.username
          else
            - var c = menu=='login' ? 'active' : ''
            a.nav(href='/admin/login', class=c) login
            - var c = menu=='register' ? 'active' : ''
            a.nav(href='/admin/register', class=c) register

        div.logo
          h1.logo
            a(href=(user ? '/admin' : '/'))
              span exqu
              span.i i
              span s
              span.i i
              span teha
              span.i i
              span ku
          p real-time, multi-player haiku composition
          - var c = menu=='new' ? 'btn-inverse' : 'btn-primary'
          if user
            a.btn.btn-large.new-poem(
              href='/admin/new', class=c) New Poem

    span.span6
      block content
      &nbsp;
